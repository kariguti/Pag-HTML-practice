function toggleNav() {
    var navBar = document.getElementById("nav");
  if (navBar.style.display === "block") {
      navBar.style.display = "none"; // Ocultar la lista de enlaces
    } else {
      navBar.style.display = "block"; // Mostrar la lista de enlaces
}
}
